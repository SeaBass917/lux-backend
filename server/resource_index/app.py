"""App configuration for the resource_index app."""
from django.apps import AppConfig


class ResourceIndexConfig(AppConfig):
    """Main configuration for the resource_index app."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "resource_index"
